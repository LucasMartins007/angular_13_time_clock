import { TipoMarcacaoPipe } from './tipo-marcacao.pipe';

describe('TipoMarcacaoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoMarcacaoPipe();
    expect(pipe).toBeTruthy();
  });
});
